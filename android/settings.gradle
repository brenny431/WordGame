pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

def flutterSdkPath = new File(rootDir, '../..').absolutePath
includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

include ":app"
